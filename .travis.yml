sudo: required
services:
  - docker

script:
  - docker build -t lbranca42/node-docker .
  - docker images lbranca42/node-docker

before_deploy:
  - docker login -u luisbranca -p Nicas#2014

deploy:
  provider: script
  script: docker push lbranca42/node-docker
  on:
    branch: master
